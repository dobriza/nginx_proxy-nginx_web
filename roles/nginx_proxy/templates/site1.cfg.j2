server {
        listen 80 default_server;
        root /var/www/site1/; 
        index index.html;
	server_name {{ inventory_hostname }};
        location / {
                proxy_pass https://{{ web_name }};
        }
}
